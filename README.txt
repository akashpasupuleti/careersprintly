CareerSprintly Services Card Animation

The Services cards now use a CSS-only bottom-to-top navy reveal.
No HTML or JavaScript changes are required for this animation.

Replace your existing style.css with the included style.css.
